apply from: "GradleCommon/common.gradle"

repositories {
    // Additional repositories
}

dependencies {
    shadow("dev.aura.lib.messagestranslator:MessagesTranslator:${messagesTranslator_version}") {
        transitive = false
    }
    shadow "dev.aura.lib.version:VersionLib:${versionLib_version}"
}
